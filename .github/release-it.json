{
  "git": {
    "changelog": "git log --pretty=format:\"* %s (%h)\" ${from}...${to}",
    "commitMessage": "(release): v${version}\n${changelog}",
    "requireCleanWorkingDir": true,
    "tag": true,
    "tagAnnotation": "(release): v${version}",
    "tagName": "v${version}"
  },
  "github": {
    "draft": false,
    "preRelease": false,
    "release": true,
    "releaseName": "(release): v${version}",
    "releaseNotes": "git log $(git describe --tags --abbrev=0)..HEAD --pretty=format:'%s (%h)' ${from}...${to} -i -E --grep='^(\(maint\)|\(feat\):|\[bugfix\]|\[docs\]):*'",
    "after:release":,
    "tokenRef": "GH_ACTIONS_TOKEN"
  },
  "hooks": {
    "after:bump": "npx auto-changelog  --latest-version '${version}' --commit-limit false --template https://raw.githubusercontent.com/release-it/release-it/master/templates/keepachangelog.hbs",
    "after:release": "echo Successfully bumped release to v${version} for ${repo.repository}.",
    "before:init": "git fetch --prune --prune-tags origin"
  },
  "npm": false,
  "plugins": {
    "@release-it/bumper": {
      "in": {
        "file": "VERSION",
        "type": "text/plain"
      },
      "out": {
        "file": "VERSION",
        "type": "text/plain"
      }
    }
  }
}
