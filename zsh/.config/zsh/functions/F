#autoload
# vim: set et:ft=zsh:sw=2:st=2:ts=2:tw=100:
#
# Print the definition of one or more functions; see fn, which implements it.

emulate -L zsh

# fn reads this so its usage and errors name F when F was the command typed
local _fn_command=$0

fn "$@"
