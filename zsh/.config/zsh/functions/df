#autoload
# vim: set et:ft=zsh:sw=2:st=2:ts=2:tw=100:
#
# cd to a path below the dotfiles checkout.
#
# This shadows df(1); use `command df` for disk usage.

emulate -L zsh

builtin cd -- ${DOTFILES:-$HOME/.config/dotfiles}/$1
