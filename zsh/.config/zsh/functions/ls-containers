#autoload
# vim: set et:ft=zsh:sw=2:st=2:ts=2:tw=100:
#
# Watch the number of running docker containers.

emulate -L zsh

command watch --no-title 'figlet "containers : $(docker ps --quiet | wc -l)"'
