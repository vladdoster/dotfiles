#autoload
# vim: set et:ft=zsh:sw=2:st=2:ts=2:tw=100:
#
# Build a sandbox image and start a shell in it.
#
# With --file the image is built from that Dockerfile; otherwise an Ubuntu image is
# offered.

emulate -L zsh
zmodload zsh/zutil || return 1

local -a help verbose file tag=(dev-sandbox)
local docker_verbosity=auto

zparseopts -D -F -K -- \
  {h,-help}=help \
  {v,-verbose}=verbose \
  {f,-file}:=file \
  {t,-tag}:=tag \
  || return 1

if (( $#help )); then
  print -rC1 -- \
    "$0 [-h|--help]" \
    "$0 [-f|--file <dockerfile>] [-t|--tag <tag>] [-v|--verbose]"
  return 0
fi

(( $+commands[docker] )) || { log::error "$0: docker command not found"; return 1 }

(( $#verbose )) && docker_verbosity=plain

local container_tag=${tag[-1]}

# namespaced so it cannot collide with anything else in the shell; extra docker run
# arguments differ between the two images, so they are passed in
docker::_run-shell () {
  print -P -- "%F{green}===>%f Starting shell in ${container_tag} container"
  command docker run \
    --hostname $container_tag \
    --interactive \
    --mount source=$container_tag,destination=/home \
    --platform linux/x86_64 \
    --tty \
    "$@" \
    ${container_tag}:latest
}

if (( $#file )); then
  local dockerfile=${file[-1]:A}
  if [[ ! -f $dockerfile ]]; then
    log::error "$0: no such file: ${file[-1]}"
    return 1
  fi
  print -P -- "%F{green}===>%f Building ${container_tag} container"
  command docker buildx build \
    --build-arg "USER=${USER}" \
    --file $dockerfile \
    --load \
    --platform linux/x86_64 \
    --progress=$docker_verbosity \
    --tag ${container_tag}:latest \
    . || { log::error "$0: failed to build ${file[-1]}"; return 1 }
  docker::_run-shell --user $USER --security-opt seccomp=unconfined
  return
fi

local REPLY
read -q ${(%):-"?%s%F{cyan}==>%f%s Build Ubuntu container? [y/N]: "}
print -r --
[[ $REPLY == y ]] || return 0

command docker buildx build \
  --load \
  --platform linux/x86_64 \
  --progress=$docker_verbosity \
  --tag ${container_tag}:latest \
  - <<'EOF' || { log::error "$0: failed to build ${container_tag}"; return 1 }
  FROM amd64/ubuntu
  ENV USER docker-user
  ENV HOME /home/$USER
  ENV DEBIAN_FRONTEND noninteractive
  RUN apt-get update --quiet=2 \
   && apt-get install --assume-yes --no-install-recommends --quiet=2 \
      autoconf autogen automake \
      build-essential \
      ca-certificates cmake coreutils curl \
      file \
      git \
      make \
      openssl \
      stow subversion sudo \
      texinfo \
      wget \
      unzip \
      zsh
  RUN useradd \
    --create-home \
    --gid root --groups sudo \
    --home-dir /home/${USER} \
    --shell "$(which zsh)" \
    --uid 1001 \
    ${USER} \
   && mkdir -p /home/${USER}/.config \
   && echo "${USER} ALL=(ALL) NOPASSWD:ALL" >>/etc/sudoers \
   && passwd --delete ${USER} \
   && chown --recursive ${USER} /home/${USER}
  USER ${USER}
  WORKDIR ${HOME}
  RUN git clone https://github.com/vladdoster/dotfiles.git .config/dotfiles
  ENTRYPOINT ["zsh"]
  CMD ["-l"]
EOF

docker::_run-shell
