#autoload
# vim: set et:ft=zsh:sw=2:st=2:ts=2:tw=100:

builtin emulate -L zsh -o EXTENDED_GLOB

autoload +X -Uz colors
colors
rc=$reset_color # because I'm laaaaaazy

print -- $fg[blue]\⚛$rc $bg[cyan]$fg[black]$@$rc
