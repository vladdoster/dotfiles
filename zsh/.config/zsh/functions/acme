#autoload
# vim: set et:ft=zsh:sw=2:st=2:ts=2:tw=100:
#
# Print its arguments as a highlighted banner.

emulate -L zsh

local mark=${(%):-'%F{blue}⚛%f'} on=${(%):-'%K{cyan}%F{black}'} off=${(%):-'%f%k'}

print -r -- "${mark} ${on}${*}${off}"
