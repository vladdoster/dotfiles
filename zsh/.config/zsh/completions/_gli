#compdef gli

local service=git
local -a original_words=("${words[@]}")
words=(git log "${original_words[@]:1}")
# git log is two words where gli was one, so CURRENT shifts to match
(( CURRENT += 1 ))

_git

# vim: set et:ft=zsh:sw=2:st=2:ts=2:tw=100:
