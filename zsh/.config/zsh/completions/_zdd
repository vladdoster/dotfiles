#compdef zdd
# vim: set expandtab filetype=zsh shiftwidth=4 softtabstop=4 tabstop=4:

_arguments -C \
    '(-d --debug)'{-d,--debug}'[Enable debug mode]' \
    '(-h --help)'{-h,--help}'[Show list of command-line options]' \
    "1: :_files -W ${ZDOTDIR:-HOME/.config/zsh}/ -g '*(N:t)'"
